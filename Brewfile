# Homebrew core
brew 'awscli'
brew 'gh'
brew 'kcov' if system "uname -a | grep -q Darwin"
brew 'markdownlint-cli'
brew 'prettier'
brew 'shellcheck'

# Shellspec
tap 'shellspec/shellspec'
brew 'shellspec'
brew 'shfmt'

# Logisparte
tap 'logisparte/packages'
brew 'shell-helpers'
brew 'kano'
